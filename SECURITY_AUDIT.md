# Security Audit — opencode-bybit

Generated by: Evidence-driven audit (Tasks 1-6)
Date: 2026-06-20

---

## Finding 1: Private Key Exposed in Repository

| Field | Value |
|---|---|
| **Finding** | Unencrypted RSA private key (2048-bit) in repository |
| **Severity** | CRITICAL |
| **File** | `bybit_private.pem` (repository root) |
| **Line** | N/A (entire file) |
| **Remediation** | `git rm --cached bybit_private.pem` + add to `.gitignore` + load from `$env:BYBIT_PRIVATE_KEY_PATH` |
| **Status** | **FIXED**: `git rm --cached` done. `.gitignore` created excluding `*.pem`. All 28 `.ps1` files now read path from `$env:BYBIT_PRIVATE_KEY_PATH`. Placeholder at `examples/bybit_private.pem.example`. |

---

## Finding 2: API Key Hardcoded in 28 Files

| Field | Value |
|---|---|
| **Finding** | API key `gkPx5g3xgL2pthIg16` hardcoded as string literal |
| **Severity** | CRITICAL |
| **File** | 28 `.ps1` files (see REFACTOR_REPORT.md: Files Modified for full list) |
| **Line** | Various (one per file, typically in the first 50 lines) |
| **Remediation** | Replace string literal with `$env:BYBIT_API_KEY` in every file |
| **Status** | **FIXED**: All 28 `.ps1` files updated. All occurrences of `gkPx5g3xgL2pthIg16`, `xfs81fCzBeUSzW2TeG`, and `Dp9eZQoC4PkALosAL1LLAoXvtZHYDZWMVR7x` removed from source. API key now loaded via `$env:BYBIT_API_KEY`. |

---

## Finding 3: Demo API Secret Exposed

| Field | Value |
|---|---|
| **Finding** | Demo HMAC secret hardcoded |
| **Severity** | HIGH |
| **File** | `paper_trading/run_paper_trader.ps1` |
| **Line** | 45 |
| **Remediation** | Replace with `$env:BYBIT_DEMO_API_SECRET` |
| **Status** | **FIXED**: Line 45 changed to `DemoApiSecret = $env:BYBIT_DEMO_API_SECRET`. |

---

## Finding 4: Demo API Key Exposed

| Field | Value |
|---|---|
| **Finding** | Demo API key hardcoded |
| **Severity** | HIGH |
| **File** | `paper_trading/run_paper_trader.ps1` |
| **Line** | 44 |
| **Remediation** | Replace with `$env:BYBIT_DEMO_API_KEY` |
| **Status** | **FIXED**: Line 44 changed to `DemoApiKey = $env:BYBIT_DEMO_API_KEY`. |

---

## Finding 5: .env File Tracked in Git

| Field | Value |
|---|---|
| **Finding** | `.env` with credentials tracked in git |
| **Severity** | HIGH |
| **File** | `.env` (repository root) |
| **Line** | All |
| **Remediation** | `git rm --cached .env` + add `.env` to `.gitignore` |
| **Status** | **FIXED**: `git rm --cached .env` done. `.gitignore` created excluding `.env`. `.env.example` updated with full template. |

---

## Finding 6: No Certificate Pinning

| Field | Value |
|---|---|
| **Finding** | API calls use default `Invoke-WebRequest` with no certificate validation override |
| **Severity** | LOW |
| **File** | All `.ps1` files with `Call-API` / `Call-Bybit` / `Invoke-WebRequest` |
| **Line** | N/A (systemic) |
| **Remediation** | Add certificate pinning for production |
| **Status** | NOT FIXED (out of scope). Would require certificate hash validation. |

---

## Summary

| Finding | Severity | Status | Remediation Date |
|---|---|---|---|
| Private key in repo | CRITICAL | **FIXED** (git-rm + gitignore + env var) | 2026-06-20 |
| API key in 28 source files | CRITICAL | **FIXED** (env var replacement) | 2026-06-20 |
| Demo API secret exposed | HIGH | **FIXED** (env var replacement) | 2026-06-20 |
| Demo API key exposed | HIGH | **FIXED** (env var replacement) | 2026-06-20 |
| .env tracked in git | HIGH | **FIXED** (git-rm + gitignore) | 2026-06-20 |
| No certificate pinning | LOW | NOT FIXED | — |

**Action items remaining**:
1. Revoke `gkPx5g3xgL2pthIg16` at Bybit immediately (key has been exposed in git history)
2. Revoke `xfs81fCzBeUSzW2TeG` / `Dp9eZQoC4PkALosAL1LLAoXvtZHYDZWMVR7x` at Bybit
3. Generate new API keys with testnet-only permissions for development
4. Set `$env:BYBIT_API_KEY`, `$env:BYBIT_PRIVATE_KEY_PATH`, `$env:BYBIT_DEMO_API_KEY`, `$env:BYBIT_DEMO_API_SECRET` in the runtime environment
